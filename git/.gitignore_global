# Run git cmds to check config for ignore
# git config --get core.excludesfile
# git config --global core.excludesfile '~/.gitignore_global'

# Any file starts with ~
*~

# MacOS 
.DS_Store

# Java
*.project
*.classpath
*.class
*.iml
*.idea/
*.gitignore.bak
*.settings
*.settings/
*/target
*/target/
